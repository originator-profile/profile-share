name: build
description: Build
runs:
  using: composite
  steps:
    - run: pnpm build
      shell: bash
    - uses: actions/upload-artifact@v3
      with:
        path: apps/web-ext/web-ext-artifacts*
