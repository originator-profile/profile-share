name: build
description: Build
runs:
  using: composite
  steps:
    - run: yarn build
      shell: bash
    - uses: actions/upload-artifact@v3.1.1
      with:
        path: apps/web-ext/web-ext-artifacts*
