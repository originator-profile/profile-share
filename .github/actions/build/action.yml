name: build
description: Build
runs:
  using: composite
  steps:
    - run: pnpm build
      shell: bash
    - uses: actions/upload-artifact@v4
      with:
        name: web-ext
        path: apps/web-ext/web-ext-artifacts*
