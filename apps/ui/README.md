# Profile Registry UI

レジストリのフロントエンドのソースコードです。

- ビルドは **`apps/registry` ディレクトリで** `yarn ui:build` を実行してください。
- 依存パッケージのインストールはこのディレクトリで行ってください（フロントエンド、バックエンドどちらの依存パッケージなのか明確にするため）

フレームワークとして[vite](https://vitejs.dev/)を使っています。
その設定ファイルは apps/registry/ 配下にあります。
