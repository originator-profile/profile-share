#!/usr/bin/env node

require("esbuild-register");

process.env.TS_NODE_DEV = "true";

(async () => {
  const oclif = await import("@oclif/core");
  await oclif.execute({ type: "cjs", dir: __filename });
})();
