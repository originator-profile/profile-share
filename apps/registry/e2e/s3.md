**TODO: [組織によるロゴの登録APIの実装](https://github.com/originator-profile/profile/issues/801)で更新する**

# ファイルアップロードのテスト

Step1.

[aws-cli](https://github.com/aws/aws-cli)をインストールする。例として、Ubuntu(WSL含む)の場合`apt install awscli`, macOSの場合`brew install awscli`

Step2.

```diff
diff --git a/apps/registry/package.json b/apps/registry/package.json
index a315c10..dbed7ea 100644
--- a/apps/registry/package.json
+++ b/apps/registry/package.json
@@ -18,10 +18,9 @@
     "build:ui": "run-s ui:clean ui:build",
     "build:cli": "oclif manifest",
     "build:api": "bin/run openapi-gen",
-    "dev": "dotenv -e .env.development run-p dev:{db,build}",
-    "dev:db": "docker compose --env-file .env.development up db",
+    "dev": "dotenv -e .env.development run-p dev:{docker,build}",
     "//TODO:gh-770:dev:docker": "docker compose --env-file .env.development up db mailpit",
-    "//TODO:gh-801:dev:dokcer": "docker compose --env-file .env.development up db minio",
+    "dev:docker": "docker compose --env-file .env.development up db minio",
     "dev:build": "tsup src --watch --ignore-watch src/router.ts --onSuccess 'run-p dev:{docs,server}'",
     "dev:docs": "run-s build:{cli,api} docs",
     "dev:server": "run-s dev:{prisma,start}",
```

Step3.

```
$ yarn dev
```

Step4.

```console
$ AWS_ACCESS_KEY_ID=root AWS_SECRET_ACCESS_KEY=kHslkqy4n2hMDvQabcwkOl_NCqdX1M2QEX_XjGe807o aws --endpoint-url http://localhost:19000 s3api create-bucket --bucket test-bucket
{
    "Location": "/test-bucket"
}
$ cat << EOF > test-file
test file
EOF
$ AWS_ACCESS_KEY_ID=root AWS_SECRET_ACCESS_KEY=kHslkqy4n2hMDvQabcwkOl_NCqdX1M2QEX_XjGe807o aws --endpoint-url http://localhost:19000 s3 cp test-file s3://test-bucket/
$ AWS_ACCESS_KEY_ID=root AWS_SECRET_ACCESS_KEY=kHslkqy4n2hMDvQabcwkOl_NCqdX1M2QEX_XjGe807o aws --endpoint-url http://localhost:19000 s3 ls s3://test-bucket/
2023-09-04 13:26:06         10 test-file
```

Step5.

http://localhost:19001/ にブラウザして確認。`Username`として`root`、`Password`として`kHslkqy4n2hMDvQabcwkOl_NCqdX1M2QEX_XjGe807o`を入力しログインすること
