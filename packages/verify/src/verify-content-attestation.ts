// 署名を検証する
// ajv の検証
// allowedUrl, allowedOrigin の検証
// digestSRI の検証
// target integrity の検証

// function validateWithJSONSchema(jsonSchema: any, vc: any): boolean {
//   return true;
// }

// function validateOrigin(origin: string, allowedOrigin: string): boolean {
//   return origin === allowedOrigin;
// }

// function validateUrl(url: string, allowedUrl: string): boolean {
//   return url === allowedUrl;
// }

// function validateDigestSRI(sri: string): boolean {
//   return /^sha256-[A-Za-z0-9+/]{43}=$/.test(sri);
// }

// function validateTargetIntegrity(
//   targetIntegrity: string,
//   sri: string,
// ): boolean {
//   return targetIntegrity === sri;
// }
