{
  "addons": [{ "plan": "heroku-postgresql" }],
  "env": {
    "APP_URL": {
      "description": "アプリケーションのURL (形式: `https://<ホスト名>`)"
    },
    "VITE_AUTH0_DOMAIN": {
      "description": "Auth0のテナントのドメイン名 (Auth0 dashboard -> Application -> Settings -> Domain)"
    },
    "VITE_AUTH0_CLIENT_ID": {
      "description": "Auth0 Client ID (Auth0 dashboard -> Application -> Settings -> Client ID)"
    }
  }
}
